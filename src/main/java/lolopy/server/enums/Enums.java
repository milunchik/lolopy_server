package lolopy.server.enums;

public class Enums {
    public enum Transport {
        PLANE, TRAIN, CAR, BUS
    }

    public enum FoodPlace {
        CAFE, RESTAURANT
    }

    public enum Accommodation {
        HOTEL, HOSTEL
    }

    public enum Category {
        COMPLETED, INCOMING
    }
}
